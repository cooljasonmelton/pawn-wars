
export const reducer = (state, action) => {
  switch (action.type) {
    case 'UPDATE_BOARD':
      return state
    case 'UPDATE_TURN':
      return state
    case 'UPDATE_BOARD':
      return state
    case 'UPDATE_TURN':
      return state
    default:
      return state
  }
}